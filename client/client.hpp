#pragma once
#include <Windows.h>
#include <string>

namespace client
{
	extern __forceinline std::string authentication();
	extern __forceinline std::string valid_version();
	extern __forceinline std::string activation();
	extern __forceinline std::string Dll();
}